{
    "search": {
        "description": "搜索行为",
        "type": "object",
        "items": {
            "exact_matches": {
                "description": "返回完全匹配结果",
                "type": "bool",
                "hint": "Google Lens 的「Exact matches」栏，列出收录了这张图的网页。和「AI 图片描述」相互独立，可以只开一个。两个都关会直接报错。",
                "default": true
            },
            "ai_mode": {
                "description": "返回 AI 图片描述",
                "type": "bool",
                "hint": "Google Lens 的「AI Mode」栏，用自然语言说明这张图是什么（作品、角色、出处等）。图片没被任何网页收录时它往往仍能给出答案。会让单次搜索多花 10 秒左右，且输出语言由 Google 按出口 IP 判定。",
                "default": true
            },
            "safe_search": {
                "description": "开启安全搜索过滤",
                "type": "bool",
                "hint": "默认关闭。实测开启后命中过滤的图片会直接返回 0 条结果（不是部分过滤），而且症状和「图片没被收录」一样无法分辨。关闭时插件会显式告知 Google 不要过滤，避免受出口 IP 所在地区的默认设置影响。",
                "default": false
            },
            "max_results": {
                "description": "最多返回条数",
                "type": "int",
                "hint": "Google 一页通常给 20 条以上。条数越多消息越长，按需调整。",
                "default": 10
            },
            "hl": {
                "description": "结果页语言",
                "type": "string",
                "hint": "影响 Google 返回的标题和站点名的语言。改这个不会改变匹配结果本身。",
                "options": [
                    "en",
                    "zh-CN",
                    "ja"
                ],
                "default": "en"
            },
            "complete_titles": {
                "description": "补全被截断的标题",
                "type": "bool",
                "hint": "Google 会把过长的标题截断成“xxx ...”。开启后会去抓目标页的 <title> 补全，属于尽力而为——不少站点有反爬会失败，失败时保留截断标题。会给每条被截断的结果多发一次请求。",
                "default": true
            },
            "with_ocr": {
                "description": "附带图中文字",
                "type": "bool",
                "hint": "额外调用 Lens 的 OCR 接口，把图片里识别出的文字一起发出来。纯 HTTP，开销很小。",
                "default": false
            }
        }
    },
    "browser": {
        "description": "浏览器",
        "type": "object",
        "hint": "Google 从 2025-01 起要求搜索页执行 JavaScript，所以必须用真实浏览器渲染结果页。",
        "items": {
            "headless": {
                "description": "无头运行",
                "type": "bool",
                "hint": "服务器上保持开启。只有需要人工过一次验证码时才临时关掉。",
                "default": true
            },
            "prefer_bundled_chromium": {
                "description": "强制使用 Playwright 自带 Chromium",
                "type": "bool",
                "hint": "默认会优先找系统安装的 Chrome/Chromium/Edge，找不到才回退。Docker 部署时通常只有自带的，无需改这项。",
                "default": false
            },
            "auto_install_browser": {
                "description": "自动安装浏览器",
                "type": "bool",
                "hint": "AstrBot 装插件只会装 pip 依赖，不会执行 playwright install，所以官方 Docker 镜像里浏览器二进制是缺的。开启后插件加载时会在后台自动下载（约 170MB，2~5 分钟），装到插件数据目录下，容器重建也不会丢。自己管理浏览器的话可以关掉。",
                "default": true
            },
            "install_system_deps": {
                "description": "自动安装时顺带装系统依赖库",
                "type": "bool",
                "hint": "即 playwright install --with-deps。精简镜像缺 libnss3 / libatk-1.0 / libcups 等库，不装的话浏览器下载成功也起不来。需要容器内是 root（官方镜像是），非 root 会自动跳过。会用 apt-get 装包。",
                "default": true
            },
            "install_timeout_minutes": {
                "description": "自动安装超时（分钟）",
                "type": "int",
                "hint": "要下载约 170MB 外加 apt 装库，网络慢时适当加大。",
                "default": 30
            },
            "chrome_path": {
                "description": "浏览器可执行文件路径",
                "type": "string",
                "hint": "留空自动查找。也可以用环境变量 CHROME_PATH 指定。",
                "default": ""
            },
            "proxy": {
                "description": "代理地址",
                "type": "string",
                "hint": "如 http://127.0.0.1:7897。留空则走系统代理或直连。机房 IP 触发人机验证的概率明显更高。",
                "default": ""
            },
            "timeout_seconds": {
                "description": "单步超时（秒）",
                "type": "int",
                "default": 60
            },
            "max_retries": {
                "description": "人机验证重试次数",
                "type": "int",
                "hint": "Google 的判定带随机性。重试会先清掉被标记的 cookie 再重新上传，实测能把偶发失败救回来。设 0 表示不重试。",
                "default": 2
            },
            "idle_close_minutes": {
                "description": "空闲多久关闭浏览器（分钟）",
                "type": "int",
                "hint": "浏览器常驻能省掉首次启动开销（冷启动那次要多花 20 秒左右），但会占几百 MB 内存。设 0 表示常驻不关。",
                "default": 30
            }
        }
    },
    "output": {
        "description": "消息输出",
        "type": "object",
        "items": {
            "show_index": {
                "description": "显示序号",
                "type": "bool",
                "default": true
            },
            "show_source": {
                "description": "显示站点名",
                "type": "bool",
                "default": true
            },
            "show_size": {
                "description": "显示图片尺寸",
                "type": "bool",
                "default": false
            },
            "use_forward_message": {
                "description": "使用合并转发消息",
                "type": "bool",
                "hint": "把结果打包成一条合并转发，点开才看到内容，不占聊天记录。关闭后按块逐条发送普通消息。搜图结果由插件直接投递，不受 AstrBot 全局设置里「转发消息的字数阈值」和「回复时引用发送人消息」的影响——否则结果会因为字数偶尔越过阈值而时折叠时不折叠。",
                "default": true
            },
            "merge_ai_and_exact": {
                "description": "合并图片描述与完全匹配",
                "type": "bool",
                "hint": "开启后两类结果放在同一条消息（或同一个转发节点）里；关闭则各自独立。与「使用合并转发消息」互不影响，可任意组合。",
                "default": false
            },
            "link_as_separate_message": {
                "description": "链接单独成一条消息",
                "type": "bool",
                "hint": "把每条结果的链接拆成独立消息，方便长按复制：标题与来源一条、链接一条，结果之间插一条横线分隔。仅在开启「使用合并转发消息」时生效——普通消息下逐条发送会刷屏。",
                "condition": {
                    "use_forward_message": true
                },
                "default": false
            },
            "working_hint": {
                "description": "开始搜索时的提示语",
                "type": "string",
                "hint": "留空则不发提示。一次搜索大约 20~30 秒，建议保留。",
                "default": "正在搜索，请稍候……"
            },
            "ai_header": {
                "description": "AI 描述前的抬头",
                "type": "string",
                "hint": "只在开启「返回 AI 图片描述」时使用。留空则直接输出描述文字，不加抬头。",
                "default": "【图片描述】"
            },
            "empty_text": {
                "description": "没有结果时的文案",
                "type": "string",
                "default": "没有找到完全匹配的结果"
            }
        }
    },
    "limits": {
        "description": "频率控制",
        "type": "object",
        "items": {
            "user_cooldown_seconds": {
                "description": "同一用户冷却（秒）",
                "type": "int",
                "hint": "0 = 不限制。搜图请求是串行执行的，冷却能避免排队堆积，也降低被 Google 限流的概率。",
                "default": 15
            },
            "wait_image_seconds": {
                "description": "等待补发图片（秒）",
                "type": "int",
                "hint": "只发指令没带图时，等用户补发图片的时长。0 = 不等待，直接提示带图。",
                "default": 60
            },
            "request_timeout_seconds": {
                "description": "单次搜索总超时（秒）",
                "type": "int",
                "hint": "一次搜索最多允许跑多久，超时会重置浏览器并回一条超时提示，确保用户不会只收到「正在搜索」就没了下文。正常一次 20~30 秒，含重试也够用；设 0 表示不限制（不建议）。",
                "default": 180
            }
        }
    }
}
